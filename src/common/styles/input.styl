input
textarea
button
select
a
  -webkit-tap-highlight-color rgba(0, 0, 0, 0)

input
textarea
button
select
  border 0
  font-family inherit
  font-size inherit
  width auto

  &:focus
    outline none

button
  cursor pointer
