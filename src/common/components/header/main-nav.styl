@import '../../styles/import'

.root
  a
    font(altFont)
    fs(1.25, 300)
    display block
    color brownish
    line-height 1
    padding 1rem

.non-responsive
  display flex
  margin-right 1rem

.responsive
  text-align center
  margin-bottom 2rem

  a
    fs(1.5, 300)
