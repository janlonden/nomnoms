@import '../../../styles/import'

@import '../../../styles/classes/button'
@import '../../../styles/classes/forms/add-field'
@import '../../../styles/classes/forms/button'
@import '../../../styles/classes/forms/edit-nav'
@import '../../../styles/classes/forms/header'
@import '../../../styles/classes/forms/input-text'
@import '../../../styles/classes/forms/label'
@import '../../../styles/classes/forms/recipe-error'
@import '../../../styles/classes/forms/section'

.root
  background-color purple2

.label
  color beige

.ingredients
  width 100%
  margin-bottom (spacing)vw

.ingredient
  display flex

.ingredient-amount
  flex-basis 40%

.ingredient-ingredient
  flex-basis 60%

.input-text
  placeholder(beige)
  color beige

.sort-up
  &:after
    icon('up', 'light')

.sort-down
  &:after
    icon('down', 'light')

.remove
  &:after
    icon('x', 'light')

@media (max-width: 567px)
  .ingredient
    flex-wrap wrap
    border 0

  .edit-nav
    flex-basis 100%
    display flex
    justify-content flex-end
    padding-top 1rem
    border-top 1px solid rgba(beige, .05)

@media (min-width: 567px)
  .ingredient
    border-bottom 1px solid rgba(beige, .05)
    position relative

  .ingredient-amount
    margin-right 2rem

  .ingredient-ingredient
    margin-right 150px

  .edit-nav
    position absolute
    top 1rem
    right -1rem

  .error
    border-left 1px solid rgba(beige, .1)

@media (min-width: 899px)
  .ingredients
    margin-bottom 5rem
